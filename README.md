# tmux config

Clone to `~/.config/tmux`:

```sh
git clone https://github.com/aymen-dr/tmux.git ~/.config/tmux
```

For tmux versions that do not load `~/.config/tmux/tmux.conf`, link it:

```sh
ln -sf ~/.config/tmux/tmux.conf ~/.tmux.conf
```
